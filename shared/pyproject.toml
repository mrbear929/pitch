[project]
name = "pitch-shared"
version = "0.1.0"
description = "Shared types between Pitch dispatcher and worker"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
